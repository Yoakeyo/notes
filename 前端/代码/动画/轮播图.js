// 已压缩，第一次尝试压缩代码
window.addEventListener('load', function () { var left = document.querySelector('.left'); var right = document.querySelector('.right'); var focus = document.querySelector('.focus'); focus.addEventListener('mouseenter', function () { left.style.display = 'block'; right.style.display = 'block'; clearInterval(timer); timer = null }); focus.addEventListener('mouseleave', function () { left.style.display = 'none'; right.style.display = 'none'; timer = setInterval(function () { right.click() }, 2000) }); var ul = focus.querySelector('ul'); var ol = focus.querySelector('.circle'); for (var i = 0; i < ul.children.length; i++) { var li = document.createElement('li'); li.setAttribute('index', i); ol.appendChild(li); li.addEventListener('click', function () { for (var i = 0; i < ol.children.length; i++) { ol.children[i].className = '' } this.className = 'current'; var index = this.getAttribute('index'); num = index; var focusWidth = -index * focus.offsetWidth; animate(ul, focusWidth) }) } ol.children[0].className = 'current'; var first = ul.children[0].cloneNode(true); ul.appendChild(first); var num = 0; var flag = true; right.addEventListener('click', function () { if (flag) { flag = false; if (num == ul.children.length - 1) { ul.style.left = 0; num = 0 } num++; if (num == ul.children.length - 1) { num = 0 } animate(ul, -num * focus.offsetWidth, function () { flag = true }); circleChange() } }); left.addEventListener('click', function () { if (flag) { flag = false; if (num == 0) { ul.style.left = ul.children.length - 1 * focus.offsetWidth + 'px'; num = ul.children.length - 1 } num--; if (num == ul.children.length - 1) { num = ul.children.length - 1 } animate(ul, -num * focus.offsetWidth, function () { flag = true }) } circleChange() }); function circleChange() { for (var i = 0; i < ol.children.length; i++) { ol.children[i].className = '' } ol.children[num].className = 'current' } var timer = setInterval(function () { right.click() }, 2000) })
